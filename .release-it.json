{
  "hooks": {
    "before:init": [
      "npm install",
      "npm run build",
      "npm test",
      "npm run update-readme"
    ]
  },
  "git": {
    "commitMessage": "chore: release v${version}",
    "tagName": "v${version}"
  },
  "npm": {
    "publish": true
  }
}
