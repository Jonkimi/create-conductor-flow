{
  "hooks": {
    "before:init": [
      "npm install",
      "npm run build",
      "npm test"
    ]
  },
  "git": {
    "commitMessage": "chore: release v${version}",
    "tagName": "v${version}"
  },
  "npm": {
    "publish": true
  }
}
