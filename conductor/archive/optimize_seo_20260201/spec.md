# Specification: Optimize Project SEO

## Context
The project needs better visibility in search engines and GitHub search.

## Goals
- Improve search ranking for terms like "coding agent", "mcp", "conductor", "gemini".
- Make the README more scannable and feature-focused.

## Requirements
1. Add `keywords` to `package.json`.
2. Improve `README.md` with a "Key Features" section and better introductory text.
3. Ensure "Installation" and "Usage" are clearly marked.
